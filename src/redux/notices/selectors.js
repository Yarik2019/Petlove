export const selectNotices = state => state.notices.notiesData;
export const selectIsLoadingNotices = state => state.notices.isLoading;
export const selectIsError = state => state.notices.isError;
export const selectTotalPages = state => state.notices.totalPages;
export const selectFavorites = state => state.notices.favorites;
export const selectNoticeById = state => state.notices.noticeById;
